/**
 * Ralph RLM worker plugin (v0.2 default).
 * Orchestration lives in the supervisor provider — this plugin is worker-only.
 */
export { RalphWorkerPlugin, RalphWorkerPlugin as default } from "@ralph-rlm/worker-plugin";